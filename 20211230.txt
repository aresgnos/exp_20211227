20211230 벌써 12월 30일,,,

<백엔드>
숫자는 쿼리

이미지는 다른 정보처럼 이미지를 던진다해서 표시 되지 않음.

이미지를 보여줄 수 있는 방식을 따로 만들어줘야한다.
= 이미지를 볼 수 있는 url을 전달한다.

첨부했을 때,
첨부하지 않았을 때 = undefined

const = 상수, 처음 만든 값에 +,-가 불가능(변경 불가능)
let, var = 변수, 처음 만든 값에 +,- 가능 (변경 가능)

const로 만든 변수는 뒤에서 변경하면 안되는 것들.
= 받은대로 써라

let는 나중에 변경할 수도 있는 것들

되도록이면 const로 만들어야함. 오류처리가 쉽다.

첨부 안 했을 때는 키가 4개
첨부 했을 때는 키가 8개로 구성

vue에서
물품등록 - 이미지 첨부할 때
console에서 파일이 어디에 들어있는지 봐야함.


https://antoniandre.github.io/vueper-slides/